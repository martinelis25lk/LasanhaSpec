spring:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    root: INFO
    br.com.lasanhaspec: INFO
    org.hibernate.SQL: OFF

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}